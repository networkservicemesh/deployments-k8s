apiVersion: spire.spiffe.io/v1alpha1
kind: ClusterSPIFFEID
metadata:
  name: nsm-workloads
spec:
  spiffeIDTemplate: "spiffe://nsm.cluster3/ns/{{.PodMeta.Namespace}}/pod/{{.PodMeta.Name}}"
  podSelector:
    matchLabels:
      "spiffe.io/spiffe-id": "true"
