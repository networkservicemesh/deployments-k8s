---
apiVersion: networkservicemesh.io/v1
kind: NetworkService
metadata:
  name: nse-composition
  namespace: nsm-system
spec:
  payload: ETHERNET
  matches:
    - source_selector:
        app: firewall
      routes:
        - destination_selector:
            app: passthrough-1
    - source_selector:
        app: passthrough-1
      routes:
        - destination_selector:
            app: gateway
    - routes:
        - destination_selector:
            app: firewall
