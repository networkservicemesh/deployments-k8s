---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: ns-kernel2rvlan-breakout

resources:
  - ../../../apps/nse-remote-vlan
  - ns-kernel2rvlan-breakout.yaml
  - first-iperf-s.yaml
  - netsvc.yaml

patchesStrategicMerge:
  - patch-nse.yaml